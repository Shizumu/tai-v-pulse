# 版本更新紀錄

本檔記錄可由實際程式碼與專案文件確認、且會影響使用者的變更；尚未建立版本產物的內容先放在「未發佈」，正式打包後再移入對應版本。

`0.7.0` 至 `0.9.0` 的日期依 `outputs/` 內同版本產物的建立日期記錄；若正式對外發布日期不同，需再人工更正。

## [未發佈]

## [0.9.0] - 2026-08-01

### 新增

- 監測首頁新增首次使用引導、台北時間、重新排序的探索／搜尋／直播／已收錄資訊層級，以及預設收合的公開資料搬遷區。
- 監測首頁新增公開監測資料搬遷，可匯出版本化 ZIP，內容包含已收錄頻道、公開頻道快照、影片、影片快照、直播同接樣本，以及分類／遊戲名稱人工修正；黑名單與收錄來源證據可分別選擇是否包含。
- 匯入前會預覽資料格式版本、建立時間、來源程式版本、各類筆數，以及檔案白名單、SHA-256、manifest 筆數與頻道／影片關聯完整性；檢查通過後才開放「合併」或「取代本機公開監測資料」。
- 直播時段熱圖新增統計日期、涵蓋頻道、直播場次及自動文字結論；點擊時段可查看頻道、直播標題、台北時間、最高同接與 YouTube 連結。
- 內容代表項目新增人工分類修正，可複選混合主題並確認遊戲名稱；修正另存於本機 SQLite，後續公開資料更新不會覆蓋，亦可改回自動判斷。
- 趨勢圖表新增 7～365 天與自訂期間、四項市場排行、熱門內容主題、組織範圍及固定頻道比較；篩選器會清楚標示圖表期間與固定 30 日統計的差異。
- 頻道工作區新增個人／團隊上層模式、公開團隊摘要與比較，並將 OAuth 連線管理改為按需展開；私人 Analytics 不會進入團隊公開合計。

### 改善

- 全頻道開台加強掃描由每小時三次改為台北時間 `00:05`、`01:05`、`08:05`、`12:05`、`15:05`、`18:05`～`23:05`，每日由 72 次降為 11 次；已知預告／直播輪詢、每 4 小時完整上傳掃描、配額安全線及「我的頻道」優先均保留。
- 公開資料合併會依頻道／影片 ID 與更新時間保留較新版本，歷史快照依父項目與擷取時間去重；取代模式只清理公開監測資料，私人 OAuth Analytics、Studio 匯入、手動補值、工作區選擇與個人設定不受影響。
- 匯入完成後可直接使用既有公開歷史；API Key 不會隨資料包搬移，後續 YouTube 更新仍使用目的電腦自行設定的 Key。資料包未含黑名單時會保留目的端黑名單；匯入的已收錄頻道會解除目的端同一 Channel ID 的舊黑名單狀態。
- 混合主題可顯示為「歌回 + 雜談」，但每筆內容在內容總數與占比只計一次。
- 分類證據改為人工確認、標題、YouTube 類別、影片標籤、說明文字的順序；早安台、朝活及早安配信預設辨識為雜談，明寫歌回或系統遊戲別名時由該明確主題優先。
- 遊戲辨識會結合 YouTube Gaming 類別、內建常用遊戲別名及既有人工確認的遊戲名稱；代表內容同步顯示分類依據與觀看／訂閱比排序規則。

### 安全性

- 公開監測資料包使用固定 `format_version=1`、逐表逐欄白名單及 JSONL 檔案 SHA-256；匯入拒絕未列檔案、不相容欄位、不安全 ZIP 路徑、雜湊／筆數不符、孤立外鍵與超過安全上限的資料包。
- 匯出格式不接受或產生 `.env`、API Key、OAuth JSON／token、`work/oauth`、私人 OAuth Analytics、Studio 匯入、手動補值、工作區選擇、個人設定或候選審核紀錄；不需也不應複製整個 `work/` 或 SQLite。

### 修正

- 修正單一頻道的上傳播放清單回傳 `playlistNotFound` 時會中止整批更新；現在會保留該頻道既有資料、推進掃描時間並繼續處理其他頻道。
- 「收錄規則」改為從卡片右上角直接展開可修改欄位；儲存後就地更新摘要，取消則保留原設定，並將系統唯讀資訊與可修改設定分區顯示。
- 頻道工作區的「期間觀看最高內容」改為顯示縮圖、影片標題、頻道名稱、發布／直播日期、觀看、觀看時數與訂閱淨變化；標題或整列可在新分頁開啟 YouTube，不再以 video ID 作為介面內容。
- 趨勢圖表的市場排行比較跳卡改由頁面最上層顯示，依可用空間自動向上或向下展開，避免前兩列被容器裁切；滑鼠、鍵盤焦點、行動版點擊、Esc 與關閉按鈕皆可操作。
- 「包含已確認畢業頻道」改為同一個可點擊的核取方塊標籤，固定放在「比較群組」正下方，避免寬螢幕與行動版錯位。

### 發佈產物

- 已於 2026-08-01 依正式白名單流程建立 `tai-v-pulse-0.9.0-public.zip` 與 `tai-v-pulse-0.9.0-setup.exe`；ZIP SHA-256 為 `73431e447d8eed98c778b660a2f0861b0e9a4acd3c086ff7389092016d8439b8`，Windows 安裝程式 SHA-256 為 `43e415bb7c66bf9c68e8d4d615501687b7d4046aab03b4f5116375e169b8b2d0`，均與同名校驗檔一致。

### 已知限制

- 分時加強掃描降低冷清時段的 API 消耗，但兩次掃描之間未預告便直接開播的頻道仍可能延後被發現；關機、睡眠、斷網或程式停止期間的即時同接無法回填。
- Windows 安裝程式仍未經 Authenticode 程式碼簽章；尚未在另一台乾淨 Windows 電腦完成首次安裝、執行環境自動安裝、長時間運作與真實資料升級驗收。
- 公開資料搬遷與 OAuth 的自動測試使用臨時 SQLite、假的 Google 回應或隔離安裝目錄；尚未用兩台實體電腦、接近 256 MB 上限的資料包或真實 Google 帳戶完成完整端到端驗收。

## [0.8.4] - 2026-07-29

### 新增

- 頻道工作區新增使用者自備 Google 桌面應用程式 OAuth 的唯讀連線，可自動辨識自己的頻道、同步最近最多 365 天的摘要、每日及內容 Analytics，並每日在背景更新。
- 新增立即同步、中斷授權及刪除本機 OAuth 設定；中斷時會向 Google 撤銷 refresh token，並刪除本機 token 與直接同步資料。
- 新增候選審核頁，保存每次探索的候選、命中依據、未收錄原因與人工處理狀態；查看及篩選既有候選不會再次消耗 YouTube Search API 配額。
- 新增台北時間整點前 5 分、整點及整點後 5 分的輕量開台偵測，並優先檢查「我的頻道」。
- 頻道工作區新增完整的 Studio 解析內容瀏覽器，可依日期範圍、內容或遊戲名稱、來源報表、資料列類型、內容格式、內容主題及核對狀態篩選，並提供 25／50／100 筆分頁。
- 全站頁尾新增「靜靜子Shizumum Ch. 製作」署名。

### 改善

- OAuth 連線區新增可展開的疑難排解，涵蓋 403 測試使用者、Analytics API 未啟用、測試 token 七天後失效、scope、錯誤 JSON、無資料、配額及網路問題。
- Google OAuth／Analytics 常見英文錯誤會轉為繁中原因、操作步驟與正確設定連結；原始訊息保留在可展開的技術細節。
- 「立即同步」會持續顯示同步中並每兩秒確認背景工作，直到顯示成功日期或可操作的失敗原因；OAuth 同步本身不再依賴公開監測 API Key 才能啟動。
- 工作區主流程改為直接唯讀連線；既有 Studio CSV／TSV／ZIP 匯入與手動補值完整保留，並收進「進階備援」區。OAuth、公開監測、Studio 匯入與手動補值維持可辨識的獨立來源。
- YouTube Studio 匯入增加繁體中文內容報表欄位、影片發布時間、長度、互動觀看及更多觀眾指標，預覽時會列出已使用與尚未納入摘要的欄位。
- Studio 匯入區直接列出可匯入的 CSV、TSV、ZIP、可辨識欄位、檔案大小與 ZIP 報表數上限，並明確標示目前不支援 XLSX。
- 已解析內容預設依日期由新到舊，表格中看得到的日期、內容、分類、報表與各項數值都可切換升冪／降冪；缺值維持 `—` 並固定排在有值資料之後。
- 台 V 自述比對可辨識英文字母大小寫與固定空白差異，並避免把 `台VLOG` 誤判為台 V 自述。

### 修正

- 修正按下「立即同步」後按鈕數秒內恢復、沒有持續進度或結果提示，容易被誤認為沒有反應的問題。
- 修正按下啟動後，啟動器可能持續等待長時間服務保留的輸出管線，導致必須關閉重開才能使用「停止」及看到最新執行狀態。
- 修正覆蓋安裝後桌面與開始功能表捷徑可能沿用舊圖示；安裝包會帶入版本化的最新版圖示並重新指定捷徑圖示來源。
- 修正 Studio 內容報表的「總計」列與每支影片明細重複加總。
- 修正「最近解析資料」只依寫入順序取 100 筆、前端再截成 40 筆，導致使用者無法查看及排序完整匯入內容的問題。
- 修正 390px 行動版會隱藏「清除篩選」按鈕的樣式衝突。

### 安全性

- Google OAuth 只申請 `youtube.readonly` 與 `yt-analytics.readonly`，不申請上傳、修改、刪除或收益權限；OAuth 用戶端設定與 token 使用目前 Windows 使用者的 DPAPI 加密後保存在 `work/`。
- OAuth 授權使用系統瀏覽器、一次性 state 與 PKCE；本機 API 的跨來源存取由萬用 `*` 收緊為台V Pulse 自己的 `127.0.0.1:3000`／`localhost:3000` 前端。
- 對外白名單包與診斷報告不包含 OAuth 設定、token 或直接同步的私人 Analytics。

### 發佈產物

- 已於 2026-07-29 建立正式 `tai-v-pulse-0.8.4-public.zip` 與 `tai-v-pulse-0.8.4-setup.exe`；兩者的 SHA-256 均與隨附校驗檔一致，可替代既有 `0.8.3` 對外安裝包。

### 已知限制

- Windows 安裝程式目前未經程式碼簽章，SmartScreen 可能顯示「未知發行者」；下載後應先核對隨附 SHA-256。
- 每位使用者需自行建立 Google Cloud「桌面應用程式」OAuth 用戶端，並啟用 YouTube Data API v3 與 YouTube Analytics API。若 OAuth 同意畫面維持測試狀態，Google 的 refresh token 通常會在 7 天後失效。
- 目前直接連線使用 YouTube Analytics Targeted Queries API；不包含曝光、曝光點閱率、回訪觀眾或收益，這些欄位仍可由 Studio 匯入補充。尚未使用 YouTube Reporting API。
- Studio 匯入目前只接受 CSV、TSV 或包含這些報表的 ZIP；YouTube Studio XLSX 內的額外圖表與每日總計工作表尚不會匯入。
- 內容主題與格式是台V Pulse 的規則分類，不是 YouTube Analytics 官方分類；特定遊戲以標題文字搜尋，證據不足時會顯示「未分類／未判斷」。

## [0.8.3] - 2026-07-28

### 新增

- Windows 啟動器新增「後台動態」，顯示正在探索頻道、更新直播同接、掃描最新上傳、更新頻道公開數據或手動新增頻道，並保留最近完成或未完成的工作。

### 改善

- 服務就緒時立即開啟本機網頁；啟動期間仍可手動開啟網頁或編輯 API Key。
- 本機網頁位址統一顯示為 `http://127.0.0.1:3000`。

### 修正

- 修正部分 Windows 環境無法由啟動器交給預設程式開啟網頁或檔案的問題。

## [0.8.2] - 2026-07-28

### 新增

- Windows 啟動器新增解除安裝入口；可選擇保留本機資料，或經再次確認後永久刪除。

### 改善

- 覆蓋安裝新版時保留 `.env`、`work/`、SQLite 與本機分析資料，並清除新版已淘汰的程式檔。

## [0.8.1] - 2026-07-28

### 修正

- 修正部分 Windows 電腦缺少 IANA 時區資料而無法啟動資料服務的問題；啟動器會在台V Pulse 自己的目錄安裝固定版本的 `tzdata`，不需使用者手動執行 pip。

## [0.8.0] - 2026-07-28

### 新增

- 提供免管理員權限的單檔 Windows 安裝程式，安裝到目前使用者的本機應用程式目錄。
- 新增圖形化啟動器，可啟動、停止及開啟台V Pulse、編輯 API Key，並從桌面或開始功能表進入。
- 新增固定錯誤代碼與診斷報告匯出，方便回報安裝、環境、前端及資料服務問題。

### 安全性

- Windows 安裝程式提供 SHA-256 校驗檔。
- 診斷報告不包含 `.env`、API Key、SQLite 或 Studio 原始檔，並遮蔽 Windows 使用者名稱與電腦名稱。
- 對外版首次使用需確認本機資料、YouTube 平台條款與非商用授權聲明。

### 已知限制

- 安裝程式未經程式碼簽章，Windows SmartScreen 可能顯示「未知發行者」。
- 尚未在另一台乾淨電腦完成自動安裝執行環境、長時間運作、升級及備份還原的完整實機驗收。

## [0.7.3] - 2026-07-28

### 改善

- `.env` 可辨識等號前後空白、單／雙引號、`export YOUTUBE_API_KEY=...` 及多個同名設定，並採用最後一行。
- 新增 `start-local.ps1 -CheckEnv`，只回報 API Key 設定是否可辨識，不會顯示金鑰內容。
- `.env` 可使用 UTF-8 BOM、系統偏好編碼或 CP950 讀取。

## [0.7.2] - 2026-07-28

### 修正

- 修正繁體中文 Windows PowerShell 5.1 可能因 UTF-8 編碼誤判而無法解析對外包腳本的問題。

## [0.7.1] - 2026-07-28

### 新增

- 新增可雙擊的啟動與停止入口。
- 首次啟動可檢查 Node.js 與 Python，並提供 WinGet 自動安裝、官方下載頁或取消選項。
- 尚未安裝前端套件時，可由程式詢問並自動執行安裝。

### 改善

- `.env` 不存在或尚未填入 API Key 時會自動建立並開啟設定檔。

## [0.7.0] - 2026-07-28

### 新增

- 提供第一個可由版本欄位與對外產物一致確認的 Windows 原始碼測試版。
- 提供本機台 V 頻道探索、指定收錄、排除與公開數據監測。
- 提供內容環境、趨勢圖表及多頻道工作區，並支援 YouTube Studio 報表匯入與手動補值。
- 提供頻道分類、企業／團體、標籤、活動狀態及詳細資料頁。

### 安全性

- 對外版固定使用本機 SQLite 與 30 天公開 API 快照上限，不夾帶 API Key、資料庫或 Studio 原始檔。
- 採用 PolyForm Noncommercial License 1.0.0，分享時需保留 `LICENSE`、`NOTICE` 與必要署名。

### 已知限制

- 本版僅提供原始碼 ZIP，不是 EXE 或安裝程式；使用者需自行準備 Node.js、Python 並執行 `npm install`。
- 關機、睡眠、斷網或程式停止期間的即時同接無法回填。
- 尚未完成乾淨電腦、長時間運作、升級及備份還原驗收。

## [早期歷史版本待確認] - 日期待確認

### 已知限制

- `tai-v-pulse-source.zip` 內部版本為 `0.1.0`，但檔名未標示正式版本。
- 檔名標示 `0.2`、`0.3`、`0.4` 的歷史 ZIP，內部 `package.json` 仍為 `0.1.0`，無法可靠確認它們是否為正式版本或逐版歸屬。
- 未找到 `0.5.x`、`0.6.x` 的版本產物、Git commit 或 Git tag，無法可靠還原相關版本資訊。
